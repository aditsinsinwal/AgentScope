id: stable-dedupe
name: Refactor stable deduplication
repository: repository
description: Return unique values in first-seen order and support unhashable values without mutating input.
public_test_command: [python, -m, pytest, tests]
hidden_test_command: [python, -m, pytest, .agentscope_hidden_tests]
hidden_tests: hidden
forbidden_paths: [tests/]
timeout_seconds: 60

