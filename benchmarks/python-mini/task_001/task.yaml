id: clamp-bounds
name: Correct reversed clamp bounds
repository: repository
description: Fix clamp so reversed lower/upper bounds are normalized while preserving normal behavior.
public_test_command: [python, -m, pytest, tests]
hidden_test_command: [python, -m, pytest, .agentscope_hidden_tests]
hidden_tests: hidden
forbidden_paths: [tests/]
timeout_seconds: 60

