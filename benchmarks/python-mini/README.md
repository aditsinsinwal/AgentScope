# python-mini

Five original, deterministic micro-repositories cover bug fixing, feature implementation,
behavior-preserving refactoring, API correction, and edge-case validation. Public tests ship in
each repository; hidden tests live alongside the task definition and enter the sandbox only after
the agent stops. These fixtures are intentionally small for harness validation—not a claim that
they predict production coding performance.

